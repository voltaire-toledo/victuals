{
  "$schema": "../../schemas/v1/package.schema.json",
  "id": "podman-compose",
  "title": "Podman Compose",
  "description": "Initial baseline payload definition for podman-compose.",
  "kind": "tool",
  "state": "draft",
  "category": "baseline",
  "platforms": [
    "linux"
  ],
  "dependencies": [],
  "delivery": {
    "manager": "apt",
    "package": "podman-compose",
    "scope": "system"
  },
  "actions": [
    {
      "id": "podman-compose.install",
      "phase": "install",
      "executor": "apt",
      "run": [
        "podman-compose"
      ]
    }
  ],
  "validation": [
    {
      "id": "podman-compose.command",
      "check": [
        "command",
        "podman-compose"
      ],
      "expected": "podman-compose is available",
      "required": true
    }
  ],
  "metrics": [
    "package-queued",
    "package-fetched",
    "package-installed",
    "validation-passed",
    "validation-failed"
  ]
}

