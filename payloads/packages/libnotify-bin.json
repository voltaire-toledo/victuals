{
  "$schema": "../../schemas/v1/package.schema.json",
  "id": "libnotify-bin",
  "title": "Libnotify Bin",
  "description": "Initial optional payload definition for libnotify-bin.",
  "kind": "tool",
  "state": "draft",
  "category": "optional",
  "platforms": [
    "linux"
  ],
  "dependencies": [],
  "delivery": {
    "manager": "apt",
    "package": "libnotify-bin",
    "scope": "user"
  },
  "actions": [
    {
      "id": "libnotify-bin.install",
      "phase": "install",
      "executor": "apt",
      "run": [
        "libnotify-bin"
      ]
    }
  ],
  "validation": [
    {
      "id": "libnotify-bin.command",
      "check": [
        "command",
        "libnotify-bin"
      ],
      "expected": "libnotify-bin is available",
      "required": true
    }
  ],
  "metrics": [
    "package-queued",
    "package-fetched",
    "package-installed",
    "validation-passed",
    "validation-failed"
  ]
}

