{
  "$schema": "../../schemas/v1/package.schema.json",
  "id": "podman",
  "title": "Podman",
  "description": "Initial baseline payload definition for podman.",
  "kind": "tool",
  "state": "draft",
  "category": "baseline",
  "platforms": [
    "linux"
  ],
  "dependencies": [],
  "delivery": {
    "manager": "apt",
    "package": "podman",
    "scope": "system"
  },
  "actions": [
    {
      "id": "podman.install",
      "phase": "install",
      "executor": "apt",
      "run": [
        "podman"
      ]
    }
  ],
  "validation": [
    {
      "id": "podman.command",
      "check": [
        "command",
        "podman"
      ],
      "expected": "podman is available",
      "required": true
    }
  ],
  "metrics": [
    "package-queued",
    "package-fetched",
    "package-installed",
    "validation-passed",
    "validation-failed"
  ]
}

