{
  "$schema": "../../schemas/v1/package.schema.json",
  "id": "podman-desktop",
  "title": "Podman Desktop",
  "description": "Initial baseline payload definition for podman-desktop.",
  "kind": "application",
  "state": "draft",
  "category": "baseline",
  "platforms": [
    "linux"
  ],
  "dependencies": [],
  "delivery": {
    "manager": "flatpak",
    "package": "io.podman_desktop.PodmanDesktop",
    "scope": "system"
  },
  "actions": [
    {
      "id": "podman-desktop.install",
      "phase": "install",
      "executor": "flatpak",
      "run": [
        "io.podman_desktop.PodmanDesktop"
      ]
    }
  ],
  "validation": [
    {
      "id": "podman-desktop.command",
      "check": [
        "command",
        "podman-desktop"
      ],
      "expected": "podman-desktop is available",
      "required": true
    }
  ],
  "metrics": [
    "package-queued",
    "package-fetched",
    "package-installed",
    "validation-passed",
    "validation-failed"
  ]
}

