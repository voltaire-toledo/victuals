# Victuals data

The `definitions/` directory contains target-neutral object metadata used by
the platform adapters. It is intentionally separate from executable scripts,
package-manager behavior, privilege decisions, and host-specific validation.
